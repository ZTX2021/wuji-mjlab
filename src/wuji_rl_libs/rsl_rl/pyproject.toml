[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "rsl-rl-lib"
version = "5.0.1+wuji1"
description = "Vendored rsl-rl with min_std clamp support"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["rsl_rl*"]
